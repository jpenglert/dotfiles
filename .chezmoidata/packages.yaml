packages:
  universal:
    taps: []

    brews:
      # GNU utilities (macOS versions are outdated)
      - bash
      - coreutils
      - moreutils
      - findutils
      - gnu-sed
      - grep
      - gmp

      # Networking
      - wget

      # Security
      - gnupg

      # Editors
      - vim

      # Search/file tools
      - ack
      - bat
      - fzf
      - tree
      - jq

      # Git
      - git
      - git-lfs
      - gh

      # Node.js
      - node
      - yarn
      - pnpm

      # Version managers
      - asdf
      - ruby-build
      - rbenv

      # AI tools
      - gemini-cli

    casks:
      # Fonts
      - font-meslo-lg-nerd-font

      # Cloud/containers
      - docker-desktop

      # Terminal
      - iterm2

      # Window management
      - rectangle
      - scroll-reverser

      # Security
      - 1password-cli
      - gpg-suite

      # IDEs
      - visual-studio-code

      # Git GUI
      - fork

      # Knowledge management
      - logseq

  work:
    taps:
      - atlassian/acli
      - withgraphite/tap

    brews:
      # Git tools
      - glab
      - acli
      - withgraphite/tap/graphite

      # Java/Maven
      - maven
      - maven-completion

      # Kubernetes
      - kubernetes-cli
      - kubectx

      # AWS
      - awscli

      # Terminal multiplexer
      - zellij

    casks:
      # Java runtime
      - temurin@21

      # Cloud
      - gcloud-cli

      # AI tools
      - chatgpt
      - claude
      - claude-code

      # IDEs
      - cursor

      # Database
      - mongodb-compass

      # API
      - postman
