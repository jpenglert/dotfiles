packages:
  universal:
    taps:
      - FelixKratz/formulae

    brews:
      # GNU utilities (macOS versions are outdated)
      - bash
      - coreutils
      - moreutils
      - findutils
      - gnu-sed
      - grep
      - gmp

      # Networking
      - wget

      # Security
      - gnupg

      # Editors
      - vim

      # Search/file tools
      - ack
      - bat
      - fzf
      - tree
      - jq

      # Git
      - git
      - git-lfs
      - gh

      # Node.js
      - node
      - yarn
      - pnpm

      # Version managers
      - asdf
      - mise
      - ruby-build
      - rbenv

      # Window management
      - borders

      # AI tools
      - gemini-cli

    casks:
      # Fonts
      - font-meslo-lg-nerd-font

      # Cloud/containers
      - docker-desktop

      # Terminal
      - iterm2

      # Window management
      - rectangle
      - scroll-reverser

      # Security
      - 1password-cli
      - gpg-suite

      # IDEs
      - visual-studio-code

      # Git GUI
      - fork

      # Knowledge management
      - logseq

  work:
    taps:
      - atlassian/acli
      - withgraphite/tap
      - pchalasani/tap
      - terror/tap

    brews:
      # Git tools
      - glab
      - acli
      - withgraphite/tap/graphite

      # Python package manager
      - uv

      # Claude Code tools
      - pchalasani/tap/aichat-search

      # Java/Maven
      - maven
      - maven-completion

      # Kubernetes
      - kubernetes-cli
      - kubectx

      # AWS
      - awscli

      # Terminal multiplexer
      - zellij

      # Task runner
      - just
      - terror/tap/just-lsp

    casks:
      # Java runtime
      - temurin@21

      # Cloud
      - gcloud-cli

      # AI tools
      - chatgpt
      - claude
      - claude-code

      # IDEs
      - cursor

      # Database
      - mongodb-compass

      # API
      - postman
