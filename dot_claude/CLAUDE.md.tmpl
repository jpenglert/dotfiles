{{ template "global-agent-instructions" . }}

## Claude Code

- Use `TaskCreate` and `TaskUpdate` to track non-trivial work. Do not discard tasks without Jesse's explicit approval.
- For test-driven development, use the `superpowers:test-driven-development` skill when available.
- For technical debugging, use the `superpowers:systematic-debugging` skill when available.
